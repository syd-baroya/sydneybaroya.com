import { Box, Typography, Stack } from "@mui/material";

export default function ProjectPage() {

    return (
        <Stack className="section" id="projectPage" >
            <Typography variant="h1" style={{ color: 'var(--primary-text)'}}>Project Page</Typography>
        </Stack>
    );
}