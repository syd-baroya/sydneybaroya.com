import { Paper } from "@mui/material";

export default function Project() {

    return (
        <section className="section" id="projects">
            <h1>Projects</h1>
            <Paper square={false}>Projects</Paper>
        </section>
    );
}