import { Sheet } from "@mui/joy";

export default function Project() {

    return (
        <section className="section" id="projects">
            <h1>Projects</h1>
            <Sheet sx={{ borderRadius: '4px', width: '100%', height: '50vh'}}>Projects Sheet</Sheet>
        </section>
    );
}